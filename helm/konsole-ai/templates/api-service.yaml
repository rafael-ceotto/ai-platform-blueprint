apiVersion: v1
kind: Service
metadata:
  name: {{ include "konsole-ai.fullname" . }}-api
  labels:
    {{- include "konsole-ai.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.api.service.port }}
      targetPort: http
  selector:
    {{- include "konsole-ai.selectorLabels" (dict "root" $ "component" "api") | nindent 4 }}
